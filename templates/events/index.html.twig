{% extends 'base.html.twig' %}

{% block title %}Events
{% endblock %}

{% block stylesheets %}
	{{ parent() }}
	<style>
		.card:hover {
			transform: scale(1.05);
			transition: transform 0.2s;
		}
	</style>
{% endblock %}

{% block body %}
	<h1>Events index</h1>

	<div
		class="dropdown"><!-- Dropdown code here -->
	</div>

	<div class="row">
		{% for event in events %}
			<div class="col-lg-3 col-md-4 col-sm-6 col-12 mb-4">
				<div class="card h-100">
					<img src="{{ event.image }}" class="card-img-top" alt="{{ event.name }}" style="height: 200px; object-fit: cover;">
					<div class="card-body">
						<h5 class="card-title">{{ event.name }}</h5>
						<p class="card-text">{{ event.description }}</p>
						<p class="card-text">
							<small class="text-muted">{{ event.date ? event.date|date('Y-m-d H:i') : '' }}</small>
						</p>
						<a href="{{ path('app_events_show', {'id': event.id}) }}" class="btn btn-outline-primary">More Details</a>
						<a href="{{ path('app_events_edit', {'id': event.id}) }}" class="btn btn-outline-secondary">Edit</a>
						<a href="{{ path('app_events_delete', {'id': event.id}) }}" class="btn btn-outline-danger">Delete</a>
					</div>
				</div>
			</div>
		{% else %}
			<p class="text-center">No records found</p>
		{% endfor %}
	</div>

	<a href="{{ path('app_events_new') }}" class="btn btn-outline-success">Create New</a>

{% endblock %}
